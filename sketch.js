//20160806 @fcaMedios
//laerning p5.js

function setup() {
  //createCanvas(400,400);
  createCanvas(windowWidth,windowHeight);
  background(255,0,0);
}

function draw() {
  ellipse(mouseX,mouseY,10,10)
}